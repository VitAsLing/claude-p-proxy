{
  "name": "claude-p-proxy",
  "version": "1.0.0",
  "description": "OpenAI-compatible API proxy via claude -p, use your Claude Max subscription with OpenClaw and other tools.",
  "type": "module",
  "scripts": {
    "start": "bun run src/server.ts",
    "dev": "bun run --watch src/server.ts"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "keywords": ["claude", "proxy", "openai", "openclaw", "claude-code", "bun"],
  "license": "MIT"
}
